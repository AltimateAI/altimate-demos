version: 2

sources:
  - name: postgres
    loader: Airbyte
    tables:
      - name: user
        description: User table in Postgres
        tags:
          - source
        columns:
          - name: user_id
            description: User id
            tests:
              - unique
      - name: activity
        description: Activity table in Postgres
        tags:
          - source
        columns:
          - name: activity_id
            description: Activity id
            tests:
              - unique
