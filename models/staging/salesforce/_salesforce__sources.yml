version: 2

sources:
  - name: salesforce
    loader: Airbyte
    tables:
      - name: users
        tags:
          - source
        columns:
          - name: user_id
            description: User id
            tests:
              - unique
