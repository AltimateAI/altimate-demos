version: 2

sources:
  - name: google_sheets
    loader: Airbyte
    tables:
      - name: subscription
        description: Subscription table in Google Sheets
        tags:
          - source
        columns:
          - name: user_id
            description: User id
            tests:
              - unique
